<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'CTOON') }}</title>
    @if (file_exists(public_path('build/manifest.json')) || file_exists(public_path('hot')))
      @vite(['resources/css/app.css', 'resources/js/app.js'])
    @else
      <link rel="stylesheet" href="/css/app.css">
    @endif
  </head>
  <body class="bg-white text-gray-900">
    <header class="border-b bg-white shadow-sm">
      <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between gap-4">
        <a href="{{ route('home') }}" class="text-2xl font-bold">CTOON</a>

        <form action="{{ route('comics.index') }}" method="GET" class="flex-1 mx-4">
          <div class="flex">
            <input name="search" value="{{ request('search') }}" placeholder="Search comics..." class="w-full px-3 py-2 border rounded-l-md" />
            <button class="px-4 bg-indigo-600 text-white rounded-r-md">Search</button>
          </div>
        </form>

        <nav class="flex items-center gap-3">
          @if (Route::has('login'))
            @auth
              <a href="{{ url('/dashboard') }}" class="px-4 py-2 border rounded">Dashboard</a>
              <form method="POST" action="{{ route('logout') }}" class="inline">
                @csrf
                <button type="submit" class="px-4 py-2 border rounded">Logout</button>
              </form>
            @else
              <a href="{{ route('login') }}" class="px-4 py-2 border rounded">Login</a>
              @if (Route::has('register'))
                <a href="{{ route('register') }}" class="px-4 py-2 bg-indigo-600 text-white rounded">Register</a>
              @endif
            @endauth
          @endif
        </nav>
      </div>
    </header>

    <main class="max-w-6xl mx-auto p-4">
      @yield('content')
    </main>

    <footer class="border-t mt-12 py-6 text-center text-sm text-gray-600">
      © {{ date('Y') }} CTOON
    </footer>
  </body>
</html>
