# CToon Application Configuration Example
# Copy this file as needed and update with your actual values

# ============================================
# LOCAL DEVELOPMENT SETTINGS
# ============================================

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=ctoon
DB_USERNAME=root
DB_PASSWORD=

# Java/App Settings
JAVA_VERSION=17
APP_ENV=development
APP_DEBUG=true

# ============================================
# RAILWAY / CI DEPLOYMENT SETTINGS
# ============================================
# For Railway deployments, set the following variables in Railway project settings
# or as GitHub Secrets used by the CI workflow:
#
# RAILWAY_API_KEY=<your-railway-api-key>
# RAILWAY_PROJECT_ID=<your-railway-project-id>
# DB_HOST=<your-railway-mysql-host>
# DB_PORT=3306
# DB_DATABASE=ctoon
# DB_USERNAME=<db-username>
# DB_PASSWORD=<db-password>
#
# For optional cloud object storage, configure the appropriate provider and
# update `filesystems.php` accordingly. By default, the app uses the `public`
# filesystem (local storage) which works with Railway container deployments.

# ============================================
# JWT SECURITY
# ============================================
# The JWT secret key is hardcoded in JwtTokenProvider.java
# For production, move it to environment variables:

# JWT_SECRET=your-very-long-and-secure-secret-key-minimum-256-bits-recommended
# JWT_EXPIRATION_MS=86400000  # 24 hours

# ============================================
# LOGGING
# ============================================

# Log Level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Hibernate SQL Logging
# Set in persistence.xml during development:
# <property name="hibernate.show_sql" value="false"/>
# <property name="hibernate.format_sql" value="true"/>

# ============================================
# APPLICATION RUNTIME
# ============================================

# App Name
APP_NAME=CToon

# Base URL
APP_URL=http://localhost:8080

# Server Port (for local Tomcat)
SERVER_PORT=8080

# Session Timeout (minutes)
SESSION_TIMEOUT=30
